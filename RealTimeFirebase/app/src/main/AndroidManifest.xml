<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pucpr.realtimefirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RealTimeFirebase">
        <activity android:name=".LojaView"></activity>
        <activity android:name=".CriarLoja" />
        <activity android:name=".EditarCliente" />
        <activity android:name=".ClienteView" />
        <activity android:name=".listagemCliente" />
        <activity android:name=".CriarCliente" />
        <activity android:name=".cadastro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>